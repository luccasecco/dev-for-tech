// Crie um algoritmo que converte dias em horas, quando recebe um número de dias.

function transformDaysInHours(value) {
  let result = value * 24
  console.log(`${value} dias são ${result} horas`)
}

transformDaysInHours(5) // 5 dias são 120 horas
