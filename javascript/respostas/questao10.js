// Utilizando os conceitos que estudamos, vamos criar um programa que irá nos informar os dias de aula para a nossa turma.

function daysOfCourse(day) {
  if (day !== 'sábado' && day !== 'domingo') {
    console.log('Temos aula')
  }
}
